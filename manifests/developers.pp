package { 'emacs':
        ensure => installed,
}
package { 'jed':
        ensure => installed,
}
package { 'subversion':
        ensure => installed,
}
package { 'git':
        ensure => installed,
}
